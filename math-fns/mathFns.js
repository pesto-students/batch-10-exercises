
function sqrt(num) {
  return Math.sqrt(num);
}

function power(base, raisedTo) {
  return Math.pow(base, raisedTo);
}

function round(num) {
  return Math.round(num);
}

export {
  sqrt,
  power,
  round,
};
